<!-- comment.new.html.erb -->
<br>
<br>
<h1>Make Your Comment Below</h1>
<br>
<h3><%= "Post Title:  #{@post.title}" %>
<br>
<br><%= "Post Body: #{@post.body}" %>
<br>
<br>
<br>
<%= form_for @comment, url: {action: :create} do |f| %>
	<%= f.label :Comments %>
   	<%= f.text_field :comment %>
    <%= f.submit "create" %>
<% end %>

<!-- (@post.id, @user.id) -->
